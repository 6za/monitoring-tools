version: '3.3'
services:
  prometheus:
    container_name: 'prometheus'
    ports:
    - '9090:9090'
    image: 'prom/prometheus'
    restart: 'always'
    volumes: 
      - prometheus-conf-ext:/etc/prometheus/'
volumes:
  prometheus-conf-ext:
      external:
        name: prometheus-conf     
